make me a simple app that has a div with 2 inputs and a button. one for field and another for value. the idea is to create an app that sends an api call to a 'getinis.php' using a post variable called 'lang' to specify the language (can either be 'EN',SW,POL or GER). make it that it shows all the fields in the ini as an elegantly styled list with option buttons to either edit or delete an item. as a bonus feature make it that theres a modal that takes in a word in an input and a select input for the language and uses google translate API to find its correct translation